import { refreshIcons } from './app.js';
export function mount(selector,html){const el=document.querySelector(selector);if(el){el.innerHTML=html;refreshIcons(el);}return el;}
export function renderPublicHeader(){return `<header style="background:#fff;border-bottom:1px solid var(--border);position:sticky;top:0;z-index:20"><div class="container row between" style="height:68px"><a class="brand" href="/index.html"><span class="brand-flexi">Flexi</span><span class="brand-cash">Cash</span></a><nav class="row"><a class="btn-link" href="/index.html">Accueil</a><a class="btn-link" href="/auth/login.html">Connexion</a><a class="btn btn-primary sm" href="/auth/register.html">Créer un compte</a></nav></div></header>`;}
export function renderPublicFooter(){return `<footer style="padding:32px 0;border-top:1px solid var(--border);background:#fff"><div class="container row between wrap"><div><span class="brand-flexi">Flexi</span><span class="brand-cash">Cash</span></div><small class="muted">Édition Web de revue administrative</small></div></footer>`;}
const nav=[['dashboard.html','house','Accueil'],['send.html','send','Envoyer'],['receive.html','download','Recevoir'],['transactions.html','book-text','Transactions'],['savings.html','piggy-bank','Épargne'],['security.html','shield','Sécurité'],['settings.html','settings','Paramètres']];
export function renderClientSidebar(active=''){return `<aside class="app-sidebar"><a class="brand" href="dashboard.html"><span class="brand-flexi">Flexi</span><span style="color:#fff">Cash</span></a><nav>${nav.map(([href,icon,label])=>`<a class="${active===href.replace('.html','')?'active':''}" href="${href}"><i data-lucide="${icon}"></i> ${label}</a>`).join('')}</nav></aside>`;}
export function renderTopbar(){return `<div class="app-topbar"><strong>FlexiCash Web</strong><a class="btn btn-ghost sm" href="../auth/login.html">Compte</a></div>`;}
export function renderBottomNav(){return `<nav class="bottom-nav"><a href="dashboard.html"><i data-lucide="house"></i><span>Accueil</span></a><a href="send.html"><i data-lucide="send"></i><span>Envoyer</span></a><a href="scan.html"><i data-lucide="scan"></i><span>Scanner</span></a><a href="transactions.html"><i data-lucide="book-text"></i><span>Activité</span></a><a href="settings.html"><i data-lucide="settings"></i><span>Plus</span></a></nav>`;}
export function toast({type='info',title='',message=''}){const el=document.createElement('div');el.className=`alert ${type==='error'?'alert-danger':'alert-info'}`;el.style.cssText='position:fixed;right:18px;bottom:18px;max-width:360px;z-index:99;box-shadow:var(--shadow);background:#fff';el.innerHTML=`<strong>${escapeHtml(title)}</strong><div>${escapeHtml(message)}</div>`;document.body.appendChild(el);setTimeout(()=>el.remove(),5000);}
export function modal({title='',body='',confirm='Confirmer',cancel='Annuler',onConfirm,onCancel}={}){const wrap=document.createElement('div');wrap.style.cssText='position:fixed;inset:0;background:rgba(0,0,0,.35);display:grid;place-items:center;z-index:100;padding:20px';wrap.innerHTML=`<div class="card" style="max-width:480px;width:100%"><h3>${escapeHtml(title)}</h3><div>${body}</div><div class="row" style="justify-content:flex-end;margin-top:18px"><button class="btn btn-ghost" data-cancel>${escapeHtml(cancel)}</button><button class="btn btn-primary" data-confirm>${escapeHtml(confirm)}</button></div></div>`;document.body.appendChild(wrap);wrap.querySelector('[data-cancel]').onclick=()=>{onCancel?.();wrap.remove()};wrap.querySelector('[data-confirm]').onclick=async()=>{const result=await onConfirm?.();if(result!==false)wrap.remove()};}
function escapeHtml(v){return String(v??'').replace(/[&<>"']/g,(c)=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));}
