{
  "name": "flexicash-web-administration",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "FlexiCash Web — administrative review edition",
  "scripts": {
    "verify": "node scripts/verify-web-edition.mjs",
    "test:syntax": "node --check sw.js && node --check assets/js/app.js",
    "test": "npm run test:syntax && npm run verify"
  },
  "engines": {
    "node": ">=20"
  }
}
